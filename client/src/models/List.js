export function List(id, name, isArchieved, index, cards) {

    this.id = id;		             // String
    this.name = name;                // String
    this.isArchieved = isArchieved;  // Boolean
    this.index = index;              // Int
    //this.cards = cards;              //Array(Card)
}